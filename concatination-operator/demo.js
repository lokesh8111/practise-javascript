let a = 5, b= 10, c = "Hello";
console.log(c+" The sum of "+a+" and "+b+" is "+(a+b));

// + will act as a concatenation operator when one of the operands is a string, and it will act as an addition operator when both operands are numbers.